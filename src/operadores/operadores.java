void main() {

    int a = 1;
    int b = 2;
    double resultado = 0 ;

    // soma

    resultado = a + b;
    IO.println("soma de " + a + " + " + b + "  = " + resultado);

    // subtracao

    resultado = a - b;
    IO.println("subtracao de " + a + "- " + b + " = " + resultado);

    // multiplicacao

    resultado = a * b;
    IO.println("multiplicacao de  " + a + " * " + b + " = " + resultado);

    // divisao

    resultado = a / b;
    IO.println("divisao de " + a + "  / " + b + " = " + resultado);
}